# Claim 1 source audit

The method section defines a shared prototype set \(S\), prototype confusion
matrices \(v_{skl}\), and annotator-specific Dirichlet parameters over
prototypes. This is an architectural existence claim, not a performance claim.
