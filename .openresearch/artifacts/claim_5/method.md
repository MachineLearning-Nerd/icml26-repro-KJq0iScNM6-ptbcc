# Claim 5 method

After correctness gates, benchmark PTBCC, released DS, BWA, and FGBCC in five
clean Python processes per method. Rotate method launch order across
repetitions. Dataset loading and integrity checks occur before each timer.
Retain raw per-dataset wall/CPU samples, repetition totals, medians,
deterministic 10,000-draw bootstrap intervals, total-time ratios, CPU identity,
and accuracy-invariance checks. FGBCC's equation-preserving vectorization must
first match the authors' Aircr golden output; its timing deviation from the
legacy loop implementation is disclosed. An injected zero-time sample is the
negative control and must be rejected.
